apps:
  - script   : ./business/video.js
    name: aries-download-video
    instances: 10
    exec_mode: cluster
    watch  : true
    # env    :
    #   NODE_ENV: development
    # env_production:
    #   NODE_ENV: production